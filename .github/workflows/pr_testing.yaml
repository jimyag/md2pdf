name: "PR Testing"

on:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Use Node v16
        uses: actions/setup-node@v6
        with:
          node-version: "16.0"
          cache: yarn
      - run: yarn
      - run: yarn test
